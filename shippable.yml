# language setting
language: php

# version numbers to test
php:
    - 5.4

services:
  - redis

script: true

# after_success:
#	- sudo mv /index.php /var/www/index.php
#	- sudo mv /controllers.js /var/www/controllers.js
#	- sudo mv /index.html /var/www/index.html
